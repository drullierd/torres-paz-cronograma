export const metadata = {
  title: "Dashboard Torres Paz",
  description: "Control de avance de obra"
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
